NAME=xolocalvendors/nodebox
VERSION=$$(git rev-parse --short HEAD)

docker-build:
	docker build -t $(NAME):$(VERSION) .
	docker tag -f $(NAME):$(VERSION) $(NAME):latest

docker-push:
	docker push $(NAME)

docker-clean:
	docker rmi -f $(NAME)

.PHONY: docker-build docker-push docker-clean